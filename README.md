# lbsm

The Linear Blend Skinning Model.

* Stores only one skinning model. It's not a scene graph.

```
Unity<-+            +->Unity
       |            |
glTF <-+- skinmesh -+->glTF
        狭い(not scene graph)
```
